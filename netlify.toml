[build]
command = "npm config set registry https://registry.npmjs.org/ --location=project && npm install && npm run build"
publish = "dist/i-scout"

[build.environment]
NODE_VERSION = "18.15.0"
NPM_FLAGS = "--no-optional --no-fund --no-audit"
